# 个人学术主页 · Xplorer

一个极简、高性能的个人学术主页模板，采用纯前端实现，支持 GitHub Pages 一键部署。

## ✨ 特性

- 🎨 **极简设计**：深色主题 + 紫蓝渐变色，现代学术风格
- 🎯 **100% 纯前端**：无依赖，无框架，完全静态
- 🚀 **高性能**：Canvas 动态粒子特效，60fps 丝滑动画
- 📱 **完全响应式**：桌面端、平板、手机完美适配
- ⚡ **快速部署**：一键部署到 GitHub Pages
- 🎮 **易于定制**：单一配置文件 `config.js`，修改即用
- 🔗 **多页面**：主页、代码仓库、文章三大版块

## 📁 项目结构

```
.
├── index.html           # 主页
├── repositories.html    # 代码仓库页
├── articles.html        # 文章页
├── config.js            # 配置文件（唯一需要修改的地方）
├── avatar.jpg           # 个人头像
├── .github/
│   └── workflows/
│       └── deploy.yml   # GitHub Actions 自动部署配置
├── deploy.ps1           # PowerShell 快速部署脚本
├── deploy.bat           # Batch 快速部署脚本
├── DEPLOYMENT.md        # 详细部署指南
└── README.md            # 本文件
```

## 🚀 快速开始

### 本地预览

1. 双击 `index.html` 直接打开，或者
2. 使用 Python 启动本地服务器：
   ```bash
   python -m http.server 8000
   ```
   然后访问 `http://localhost:8000`

### 修改个人信息

编辑 `config.js` 文件，修改以下内容：

```javascript
const siteConfig = {
  profile: {
    name: 'your_name',           // 你的名字
    subtitle: 'your_subtitle',   // 副标题
    avatar: 'avatar.jpg',        // 头像文件
    email: 'your_email@example.com',
    githubUrl: 'https://github.com/yourname',
  },
  about: {
    title: '关于我',
    intro: '你的自我介绍...',
    passion: '你的热情...',
    motto: '你的格言...',
  },
  repositories: [
    // 添加你的项目...
  ],
  articles: [
    // 添加你的文章...
  ],
};
```

## 🌐 部署到 GitHub Pages

### 方式一：快速部署脚本（推荐）

**Windows 用户**：
```bash
./deploy.ps1
```

**macOS/Linux 用户**：
```bash
bash deploy.ps1
```

### 方式二：手动部署

1. 创建一个新仓库：`username.github.io`（将 `username` 替换为你的 GitHub 用户名）

2. 克隆仓库：
   ```bash
   git clone https://github.com/username/username.github.io.git
   cd username.github.io
   ```

3. 复制所有文件到仓库目录

4. 提交和推送：
   ```bash
   git add .
   git commit -m "初始化个人主页"
   git push origin main
   ```

5. 访问 `https://username.github.io` 查看你的网站

详见 [DEPLOYMENT.md](./DEPLOYMENT.md)

## 📝 配置说明

### 添加项目到代码仓库页

在 `config.js` 的 `repositories` 数组中添加：

```javascript
{
  icon: '🚀',
  title: '项目名称',
  desc: '项目描述',
  tags: ['标签1', '标签2'],
  url: 'https://github.com/username/repo',
}
```

### 添加文章

在 `config.js` 的 `articles` 数组中添加：

```javascript
{
  title: '文章标题',
  date: '2024年12月4日',
  desc: '文章摘要',
  tags: ['标签1', '标签2'],
  url: 'https://example.com/article' // 或 '#' 表示开发中
}
```

## 🎨 自定义样式

所有主要样式定义在各 HTML 文件的 `<style>` 标签中。

常用颜色值：
- 背景：`#0f111a`
- 主色调：`#6366f1` 到 `#8b5cf6`（紫蓝渐变）
- 文字：`#e0e0e0`
- 辅助文字：`#9095a8`

## 🔧 技术栈

- **HTML5**：语义化标签
- **CSS3**：Flexbox、Grid、渐变、动画
- **Vanilla JavaScript**：无依赖、零框架
- **Canvas API**：粒子特效实现
- **Google Fonts**：Inter 字体

## 📱 浏览器兼容性

- ✅ Chrome / Chromium 88+
- ✅ Firefox 85+
- ✅ Safari 14+
- ✅ Edge 88+

## 📄 许可证

MIT License - 可自由使用、修改和分发

## 📧 支持与反馈

如有问题或建议，欢迎提交 Issue 或 Pull Request。

---

**快乐创作，享受分享！** ✨
